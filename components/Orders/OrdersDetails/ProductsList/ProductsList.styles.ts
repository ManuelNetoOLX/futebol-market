import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    gap: 16,
  },
  sectionName: {
    fontSize: 14,
    fontWeight: 700,
    color: "#0D0D0D",
    fontFamily: "PoppinsBold",
  },
  wrapperCards: {
    gap: 16,
  },
});
